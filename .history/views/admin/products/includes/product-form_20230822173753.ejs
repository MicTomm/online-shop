 <!-- multipart/form-data => per file upload-->
        <!-- avendo nel form un file upload, aggiungere un elemento input type="hidden" per gestire il csrf token non funziona.
             Il package csurf offre l'alternativa di gestire questo caso specificando il csrf token come parametro nell'URL della 
             POST request-->
             <form action="/admin/products?_csrf=<%=locals.csrfToken%>" method="POST" enctype="multipart/form-data">
                <p>
                    <label for="title">Title</label>
                    <input type="text" name="title" id="title" required>
                </p>
                <p>
                    <label for="title">Image</label>
                    <input type="file" name="image" id="image" accept="image/png, image/jpg">
                </p>
                <p>
                    <label for="title">Summary</label>
                    <input type="text" name="summary" id="summary" maxlength="100" required>
                </p>
                <p>
                    <label for="price">Price</label>
                    <input type="number" name="price" id="price" min="0.01" step="0.01" required>
                </p>
                <p>
                    <label for="description">Description</label>
                    <textarea name="description" id="description" rows="7"></textarea>
                </p>
                <button type="reset" class="btn btn-reset">Reset</button>
                <button class="btn">Save</button>
            </form>